<?xml version="1.0" encoding="UTF-8"?>
<ListView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="WeekPlanner.Controls.WeekDayListView"> Grid.Column="0" HasUnevenRows="True" SeparatorVisibility="None" VerticalOptions="FillAndExpand" CachingStrategy="RecycleElement"&gt;

    <ListView.ItemTemplate>
        <DataTemplate>
            <ViewCell>
                <ffimageloading:CachedImage
                    VerticalOptions="Center"
                    DownsampleToViewSize="true"
                    LoadingPlaceholder="image_loading.png"
                    ErrorPlaceholder="image_loading.png"
                    Source="{Binding}"
                    Margin="0,5,0,0" />
            </ViewCell>
        </DataTemplate>
    </ListView.ItemTemplate>
</ListView>
